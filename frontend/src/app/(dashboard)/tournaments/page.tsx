"use client";

import ProtectedRoute from "@/components/ProtectedRoute";
import React from "react";

const TournamentPage = () => {
	return <div>TournamentPage</div>;
};

export default ProtectedRoute(TournamentPage);
